// Generated by kibo from features. Do not edit by hand.
// Templates: kibo-template-viper 1.2.1 (MIT).
// Runtime: this file imports `@digitalsubstrate/dsviper`, distributed under
// LicenseRef-DigitalSubstrate-Commercial-1.2. Commercial use
// requires a Commercial Licence from Digital Substrate.
// Generated from Features.dsm.json by kibo-1.2.11.jar

/**
 * TypeScript classes for concepts, clubs, enums, structs and containers of features.
 */

import dsviper from "@digitalsubstrate/dsviper";
import * as mt from "./value_type.js";
import * as md from "./definitions.js";

export class Proxy<V extends dsviper.Value = dsviper.Value> {

    readonly vprValue: V;

    constructor(vprValue: V) {
        this.vprValue = vprValue;
    }

    equals(other: Proxy): boolean {
        return this.vprValue.equals(other.vprValue);
    }

    compareTo(other: Proxy): number {
        return this.vprValue.compare(other.vprValue);
    }

    toString(): string {
        return this.vprValue.toString();
    }

    hexdigest(): string {
        return dsviper.Value.hexdigest(this.vprValue);
    }

    encode(streamCodecInstancing?: dsviper.StreamCodecInstancing): dsviper.ValueBlob {
        return dsviper.Value.encode(this.vprValue, streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY);
    }

    write(streamWriting: dsviper.StreamWriting): void {
        dsviper.Value.write(this.vprValue, streamWriting);
    }
}

// AnyConceptKey
export class AnyConceptKey extends Proxy<dsviper.ValueKey> {

    constructor(value: dsviper.ValueKey) {
        if (!value.typeKey().isAnyConcept()) {
            throw new TypeError("value is not a Key for any_concept");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): AnyConceptKey {
        return new AnyConceptKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptA, md.RuntimeIds.Test_ConceptB, md.RuntimeIds.Test_ConceptC, md.RuntimeIds.Test_ConceptCoverage, md.RuntimeIds.Test_ConceptD].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptA)) { return `${instance}:AnyConceptKey(Test::ConceptAKey)`; }
        if (rid.equals(md.RuntimeIds.Test_ConceptB)) { return `${instance}:AnyConceptKey(Test::ConceptBKey)`; }
        if (rid.equals(md.RuntimeIds.Test_ConceptC)) { return `${instance}:AnyConceptKey(Test::ConceptCKey)`; }
        if (rid.equals(md.RuntimeIds.Test_ConceptCoverage)) { return `${instance}:AnyConceptKey(Test::ConceptCoverageKey)`; }
        if (rid.equals(md.RuntimeIds.Test_ConceptD)) { return `${instance}:AnyConceptKey(Test::ConceptDKey)`; }
        return `${instance}:AnyConceptKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): AnyConceptKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_AnyConceptKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new AnyConceptKey(value);
    }
}

// Namespace Test
// concept Test::ConceptAKey
export class Test_ConceptAKey extends Proxy<dsviper.ValueKey> {

    constructor(identifier: dsviper.ValueKey | dsviper.ValueUUId | string) {
        if (identifier instanceof dsviper.ValueKey) {
            if (!identifier.type().equals(mt.type_Test_ConceptAKey())) {
                throw new TypeError("identifier is not a Test::ConceptAKey");
            }
            super(identifier);
        } else if (identifier instanceof dsviper.ValueUUId || typeof identifier === "string") {
            super(dsviper.ValueKey.create(mt.type_check_Test_ConceptAKey(), identifier));
        } else {
            throw new TypeError("identifier must be a ValueKey, a ValueUUId or a string");
        }
    }

    static wrap(value: dsviper.OutputValue): Test_ConceptAKey {
        return new Test_ConceptAKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    static create(): Test_ConceptAKey {
        return new Test_ConceptAKey(dsviper.ValueUUId.create());
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptA].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptA)) { return `${instance}:Test::ConceptAKey`; }
        return `${instance}:Test::ConceptAKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static fromAnyConceptKey(key: AnyConceptKey): Test_ConceptAKey | null {
        const rid = key.runtimeId();
        if (rid.equals(md.RuntimeIds.Test_ConceptA)) {
            return new Test_ConceptAKey(key.instanceId());
        }
        return null;
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_ConceptAKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_ConceptAKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_ConceptAKey(value);
    }
}

// concept Test::ConceptBKey
export class Test_ConceptBKey extends Proxy<dsviper.ValueKey> {

    constructor(identifier: dsviper.ValueKey | dsviper.ValueUUId | string) {
        if (identifier instanceof dsviper.ValueKey) {
            if (!identifier.type().equals(mt.type_Test_ConceptBKey())) {
                throw new TypeError("identifier is not a Test::ConceptBKey");
            }
            super(identifier);
        } else if (identifier instanceof dsviper.ValueUUId || typeof identifier === "string") {
            super(dsviper.ValueKey.create(mt.type_check_Test_ConceptBKey(), identifier));
        } else {
            throw new TypeError("identifier must be a ValueKey, a ValueUUId or a string");
        }
    }

    static wrap(value: dsviper.OutputValue): Test_ConceptBKey {
        return new Test_ConceptBKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    static create(): Test_ConceptBKey {
        return new Test_ConceptBKey(dsviper.ValueUUId.create());
    }
    asConceptCKey(): Test_ConceptCKey | null {
        if (this.vprValue.isMember(mt.type_check_Test_ConceptCKey())) {
            return new Test_ConceptCKey(this.vprValue.toMemberKey(mt.type_check_Test_ConceptCKey()));
        }
        return null;
    }
    static fromConceptCKey(key: Test_ConceptCKey): Test_ConceptBKey {
        return new Test_ConceptBKey(key.vprValue.toMemberKey(mt.type_check_Test_ConceptBKey()));
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptB, md.RuntimeIds.Test_ConceptC].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptC)) { return `${instance}:Test::ConceptBKey(Test::ConceptCKey)`; }
        if (rid.equals(md.RuntimeIds.Test_ConceptB)) { return `${instance}:Test::ConceptBKey`; }
        return `${instance}:Test::ConceptBKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static fromAnyConceptKey(key: AnyConceptKey): Test_ConceptBKey | null {
        const rid = key.runtimeId();
        if (rid.equals(md.RuntimeIds.Test_ConceptB)) {
            return new Test_ConceptBKey(key.instanceId());
        }
        return null;
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_ConceptBKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_ConceptBKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_ConceptBKey(value);
    }
}

// concept Test::ConceptCKey
export class Test_ConceptCKey extends Proxy<dsviper.ValueKey> {

    constructor(identifier: dsviper.ValueKey | dsviper.ValueUUId | string) {
        if (identifier instanceof dsviper.ValueKey) {
            if (!identifier.type().equals(mt.type_Test_ConceptCKey())) {
                throw new TypeError("identifier is not a Test::ConceptCKey");
            }
            super(identifier);
        } else if (identifier instanceof dsviper.ValueUUId || typeof identifier === "string") {
            super(dsviper.ValueKey.create(mt.type_check_Test_ConceptCKey(), identifier));
        } else {
            throw new TypeError("identifier must be a ValueKey, a ValueUUId or a string");
        }
    }

    static wrap(value: dsviper.OutputValue): Test_ConceptCKey {
        return new Test_ConceptCKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    static create(): Test_ConceptCKey {
        return new Test_ConceptCKey(dsviper.ValueUUId.create());
    }

    toParentKey(): Test_ConceptBKey {
        return new Test_ConceptBKey(this.vprValue.toParentKey());
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptC].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptC)) { return `${instance}:Test::ConceptCKey`; }
        return `${instance}:Test::ConceptCKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static fromAnyConceptKey(key: AnyConceptKey): Test_ConceptCKey | null {
        const rid = key.runtimeId();
        if (rid.equals(md.RuntimeIds.Test_ConceptC)) {
            return new Test_ConceptCKey(key.instanceId());
        }
        return null;
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_ConceptCKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_ConceptCKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_ConceptCKey(value);
    }
}

// concept Test::ConceptCoverageKey
export class Test_ConceptCoverageKey extends Proxy<dsviper.ValueKey> {

    constructor(identifier: dsviper.ValueKey | dsviper.ValueUUId | string) {
        if (identifier instanceof dsviper.ValueKey) {
            if (!identifier.type().equals(mt.type_Test_ConceptCoverageKey())) {
                throw new TypeError("identifier is not a Test::ConceptCoverageKey");
            }
            super(identifier);
        } else if (identifier instanceof dsviper.ValueUUId || typeof identifier === "string") {
            super(dsviper.ValueKey.create(mt.type_check_Test_ConceptCoverageKey(), identifier));
        } else {
            throw new TypeError("identifier must be a ValueKey, a ValueUUId or a string");
        }
    }

    static wrap(value: dsviper.OutputValue): Test_ConceptCoverageKey {
        return new Test_ConceptCoverageKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    static create(): Test_ConceptCoverageKey {
        return new Test_ConceptCoverageKey(dsviper.ValueUUId.create());
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptCoverage].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptCoverage)) { return `${instance}:Test::ConceptCoverageKey`; }
        return `${instance}:Test::ConceptCoverageKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static fromAnyConceptKey(key: AnyConceptKey): Test_ConceptCoverageKey | null {
        const rid = key.runtimeId();
        if (rid.equals(md.RuntimeIds.Test_ConceptCoverage)) {
            return new Test_ConceptCoverageKey(key.instanceId());
        }
        return null;
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_ConceptCoverageKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_ConceptCoverageKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_ConceptCoverageKey(value);
    }
}

// concept Test::ConceptDKey
export class Test_ConceptDKey extends Proxy<dsviper.ValueKey> {

    constructor(identifier: dsviper.ValueKey | dsviper.ValueUUId | string) {
        if (identifier instanceof dsviper.ValueKey) {
            if (!identifier.type().equals(mt.type_Test_ConceptDKey())) {
                throw new TypeError("identifier is not a Test::ConceptDKey");
            }
            super(identifier);
        } else if (identifier instanceof dsviper.ValueUUId || typeof identifier === "string") {
            super(dsviper.ValueKey.create(mt.type_check_Test_ConceptDKey(), identifier));
        } else {
            throw new TypeError("identifier must be a ValueKey, a ValueUUId or a string");
        }
    }

    static wrap(value: dsviper.OutputValue): Test_ConceptDKey {
        return new Test_ConceptDKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    static create(): Test_ConceptDKey {
        return new Test_ConceptDKey(dsviper.ValueUUId.create());
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptD].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptD)) { return `${instance}:Test::ConceptDKey`; }
        return `${instance}:Test::ConceptDKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static fromAnyConceptKey(key: AnyConceptKey): Test_ConceptDKey | null {
        const rid = key.runtimeId();
        if (rid.equals(md.RuntimeIds.Test_ConceptD)) {
            return new Test_ConceptDKey(key.instanceId());
        }
        return null;
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_ConceptDKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_ConceptDKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_ConceptDKey(value);
    }
}

// club Test::EmptyKlubKey
export class Test_EmptyKlubKey extends Proxy<dsviper.ValueKey> {

    constructor(value: dsviper.ValueKey) {
        if (!value.type().equals(mt.type_Test_EmptyKlubKey())) {
            throw new TypeError("value is not a Test::EmptyKlubKey");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): Test_EmptyKlubKey {
        return new Test_EmptyKlubKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }


    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        return `Test::EmptyKlubKey(${instance})`;
    }

    toString(): string {
        return this.description();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_EmptyKlubKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_EmptyKlubKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_EmptyKlubKey(value);
    }
}

// club Test::KlubKey
export class Test_KlubKey extends Proxy<dsviper.ValueKey> {

    constructor(value: dsviper.ValueKey) {
        if (!value.type().equals(mt.type_Test_KlubKey())) {
            throw new TypeError("value is not a Test::KlubKey");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): Test_KlubKey {
        return new Test_KlubKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    asConceptCKey(): Test_ConceptCKey | null {
        if (this.runtimeId().equals(md.RuntimeIds.Test_ConceptC)) {
            return new Test_ConceptCKey(dsviper.ValueKey.create(mt.type_check_Test_ConceptCKey(), this.instanceId()).toMemberKey(mt.type_check_Test_ConceptCKey()));
        }
        return null;
    }
    asConceptDKey(): Test_ConceptDKey | null {
        if (this.runtimeId().equals(md.RuntimeIds.Test_ConceptD)) {
            return new Test_ConceptDKey(dsviper.ValueKey.create(mt.type_check_Test_ConceptDKey(), this.instanceId()).toMemberKey(mt.type_check_Test_ConceptDKey()));
        }
        return null;
    }
    static fromConceptCKey(key: Test_ConceptCKey): Test_KlubKey {
        return new Test_KlubKey(key.vprValue.toClubKey(mt.type_check_Test_KlubKey()));
    }
    static fromConceptDKey(key: Test_ConceptDKey): Test_KlubKey {
        return new Test_KlubKey(key.vprValue.toClubKey(mt.type_check_Test_KlubKey()));
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [md.RuntimeIds.Test_ConceptC, md.RuntimeIds.Test_ConceptD].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        if (rid.equals(md.RuntimeIds.Test_ConceptC)) { return `${instance}:Test::KlubKey(Test::ConceptCKey)`; }
        if (rid.equals(md.RuntimeIds.Test_ConceptD)) { return `${instance}:Test::KlubKey(Test::ConceptDKey)`; }
        return `Test::KlubKey(${instance})`;
    }

    toString(): string {
        return this.description();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_KlubKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_Test_KlubKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_KlubKey(value);
    }
}

// enum Test::EnumerationE
export class Test_EnumerationE extends Proxy<dsviper.ValueEnumeration> {

    constructor(value: dsviper.ValueEnumeration) {
        if (!value.type().equals(mt.type_Test_EnumerationE())) {
            throw new TypeError("value is not a Test::EnumerationE");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): Test_EnumerationE {
        return new Test_EnumerationE(dsviper.ValueEnumeration.cast(value as dsviper.Value));
    }

    name(): string {
        return this.vprValue.name();
    }

    private static _A: Test_EnumerationE | null = null;
    static get A(): Test_EnumerationE {
        if (Test_EnumerationE._A === null) {
            Test_EnumerationE._A = new Test_EnumerationE(new dsviper.ValueEnumeration(mt.type_Test_EnumerationE(), "a"));
        }
        return Test_EnumerationE._A;
    }
    private static _B: Test_EnumerationE | null = null;
    static get B(): Test_EnumerationE {
        if (Test_EnumerationE._B === null) {
            Test_EnumerationE._B = new Test_EnumerationE(new dsviper.ValueEnumeration(mt.type_Test_EnumerationE(), "b"));
        }
        return Test_EnumerationE._B;
    }
    private static _C: Test_EnumerationE | null = null;
    static get C(): Test_EnumerationE {
        if (Test_EnumerationE._C === null) {
            Test_EnumerationE._C = new Test_EnumerationE(new dsviper.ValueEnumeration(mt.type_Test_EnumerationE(), "c"));
        }
        return Test_EnumerationE._C;
    }

    static fromStr(name: string): Test_EnumerationE {
        switch (name) {
            case "a":
                return Test_EnumerationE.A;
            case "b":
                return Test_EnumerationE.B;
            case "c":
                return Test_EnumerationE.C;
            default:
                throw new Error(`unknown enumeration case ${name}`);
        }
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_EnumerationE {
        const value = dsviper.ValueEnumeration.cast(dsviper.Value.decode(blob, mt.type_Test_EnumerationE(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_EnumerationE(value);
    }
}

// struct Test::StructureS
export class Test_StructureS extends Proxy<dsviper.ValueStructure> {

    constructor(value?: dsviper.ValueStructure | Record<string, dsviper.InputValue>) {
        if (value instanceof dsviper.ValueStructure) {
            if (!value.type().equals(mt.type_Test_StructureS())) {
                throw new TypeError("value is not a Test::StructureS");
            }
            super(value);
        } else {
            super(new dsviper.ValueStructure(mt.type_Test_StructureS(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Test_StructureS {
        return new Test_StructureS(dsviper.ValueStructure.cast(value as dsviper.Value));
    }

    get f_float(): number {
        return this.vprValue.at("f_float") as number;
    }

    set f_float(value: number) {
        this.vprValue.set("f_float", value);
    }
    get f_string(): string {
        return this.vprValue.at("f_string") as string;
    }

    set f_string(value: string) {
        this.vprValue.set("f_string", value);
    }

    copy(): Test_StructureS {
        return new Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_StructureS {
        const value = dsviper.ValueStructure.cast(dsviper.Value.decode(blob, mt.type_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_StructureS(value);
    }
}

// struct Test::StructureT
export class Test_StructureT extends Proxy<dsviper.ValueStructure> {

    constructor(value?: dsviper.ValueStructure | Record<string, dsviper.InputValue>) {
        if (value instanceof dsviper.ValueStructure) {
            if (!value.type().equals(mt.type_Test_StructureT())) {
                throw new TypeError("value is not a Test::StructureT");
            }
            super(value);
        } else {
            super(new dsviper.ValueStructure(mt.type_Test_StructureT(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Test_StructureT {
        return new Test_StructureT(dsviper.ValueStructure.cast(value as dsviper.Value));
    }

    get field_string(): string {
        return this.vprValue.at("field_string") as string;
    }

    set field_string(value: string) {
        this.vprValue.set("field_string", value);
    }
    get field_structure_s(): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.at("field_structure_s"));
    }

    set field_structure_s(value: Test_StructureS) {
        this.vprValue.set("field_structure_s", value.vprValue);
    }

    copy(): Test_StructureT {
        return new Test_StructureT(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_StructureT {
        const value = dsviper.ValueStructure.cast(dsviper.Value.decode(blob, mt.type_Test_StructureT(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_StructureT(value);
    }
}

// struct Test::StructureU
export class Test_StructureU extends Proxy<dsviper.ValueStructure> {

    constructor(value?: dsviper.ValueStructure | Record<string, dsviper.InputValue>) {
        if (value instanceof dsviper.ValueStructure) {
            if (!value.type().equals(mt.type_Test_StructureU())) {
                throw new TypeError("value is not a Test::StructureU");
            }
            super(value);
        } else {
            super(new dsviper.ValueStructure(mt.type_Test_StructureU(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Test_StructureU {
        return new Test_StructureU(dsviper.ValueStructure.cast(value as dsviper.Value));
    }

    get f_bool(): boolean {
        return this.vprValue.at("f_bool") as boolean;
    }

    set f_bool(value: boolean) {
        this.vprValue.set("f_bool", value);
    }
    get f_uint8(): number {
        return this.vprValue.at("f_uint8") as number;
    }

    set f_uint8(value: number) {
        this.vprValue.set("f_uint8", value);
    }
    get f_uint16(): number {
        return this.vprValue.at("f_uint16") as number;
    }

    set f_uint16(value: number) {
        this.vprValue.set("f_uint16", value);
    }
    get f_uint32(): number {
        return this.vprValue.at("f_uint32") as number;
    }

    set f_uint32(value: number) {
        this.vprValue.set("f_uint32", value);
    }
    get f_uint64(): bigint {
        return this.vprValue.at("f_uint64") as bigint;
    }

    set f_uint64(value: bigint) {
        this.vprValue.set("f_uint64", value);
    }
    get f_int8(): number {
        return this.vprValue.at("f_int8") as number;
    }

    set f_int8(value: number) {
        this.vprValue.set("f_int8", value);
    }
    get f_int16(): number {
        return this.vprValue.at("f_int16") as number;
    }

    set f_int16(value: number) {
        this.vprValue.set("f_int16", value);
    }
    get f_int32(): number {
        return this.vprValue.at("f_int32") as number;
    }

    set f_int32(value: number) {
        this.vprValue.set("f_int32", value);
    }
    get f_int64(): bigint {
        return this.vprValue.at("f_int64") as bigint;
    }

    set f_int64(value: bigint) {
        this.vprValue.set("f_int64", value);
    }
    get f_float(): number {
        return this.vprValue.at("f_float") as number;
    }

    set f_float(value: number) {
        this.vprValue.set("f_float", value);
    }
    get f_double(): number {
        return this.vprValue.at("f_double") as number;
    }

    set f_double(value: number) {
        this.vprValue.set("f_double", value);
    }
    get f_blob_id(): dsviper.ValueBlobId {
        return this.vprValue.at("f_blob_id") as dsviper.ValueBlobId;
    }

    set f_blob_id(value: dsviper.ValueBlobId) {
        this.vprValue.set("f_blob_id", value);
    }
    get f_commit_id(): dsviper.ValueCommitId {
        return this.vprValue.at("f_commit_id") as dsviper.ValueCommitId;
    }

    set f_commit_id(value: dsviper.ValueCommitId) {
        this.vprValue.set("f_commit_id", value);
    }
    get f_uuid(): dsviper.ValueUUId {
        return this.vprValue.at("f_uuid") as dsviper.ValueUUId;
    }

    set f_uuid(value: dsviper.ValueUUId) {
        this.vprValue.set("f_uuid", value);
    }
    get f_string(): string {
        return this.vprValue.at("f_string") as string;
    }

    set f_string(value: string) {
        this.vprValue.set("f_string", value);
    }
    get f_blob(): dsviper.ValueBlob {
        return this.vprValue.at("f_blob") as dsviper.ValueBlob;
    }

    set f_blob(value: dsviper.ValueBlob) {
        this.vprValue.set("f_blob", value);
    }
    get f_vec(): Vec_uint8_2 {
        return Vec_uint8_2.wrap(this.vprValue.at("f_vec"));
    }

    set f_vec(value: Vec_uint8_2) {
        this.vprValue.set("f_vec", value.vprValue);
    }
    get f_mat(): Mat_uint8_2_2 {
        return Mat_uint8_2_2.wrap(this.vprValue.at("f_mat"));
    }

    set f_mat(value: Mat_uint8_2_2) {
        this.vprValue.set("f_mat", value.vprValue);
    }
    get f_tuple(): Tuple_uint8_string {
        return Tuple_uint8_string.wrap(this.vprValue.at("f_tuple"));
    }

    set f_tuple(value: Tuple_uint8_string) {
        this.vprValue.set("f_tuple", value.vprValue);
    }
    get f_optional(): Optional_uint8 {
        return Optional_uint8.wrap(this.vprValue.at("f_optional"));
    }

    set f_optional(value: Optional_uint8) {
        this.vprValue.set("f_optional", value.vprValue);
    }
    get f_vector(): Vector_uint8 {
        return Vector_uint8.wrap(this.vprValue.at("f_vector"));
    }

    set f_vector(value: Vector_uint8) {
        this.vprValue.set("f_vector", value.vprValue);
    }
    get f_set(): Set_uint8 {
        return Set_uint8.wrap(this.vprValue.at("f_set"));
    }

    set f_set(value: Set_uint8) {
        this.vprValue.set("f_set", value.vprValue);
    }
    get f_set_s(): Set_Test_StructureS {
        return Set_Test_StructureS.wrap(this.vprValue.at("f_set_s"));
    }

    set f_set_s(value: Set_Test_StructureS) {
        this.vprValue.set("f_set_s", value.vprValue);
    }
    get f_map_s1(): Map_Test_StructureS_to_string {
        return Map_Test_StructureS_to_string.wrap(this.vprValue.at("f_map_s1"));
    }

    set f_map_s1(value: Map_Test_StructureS_to_string) {
        this.vprValue.set("f_map_s1", value.vprValue);
    }
    get f_map_s2(): Map_string_to_Test_StructureS {
        return Map_string_to_Test_StructureS.wrap(this.vprValue.at("f_map_s2"));
    }

    set f_map_s2(value: Map_string_to_Test_StructureS) {
        this.vprValue.set("f_map_s2", value.vprValue);
    }
    get f_xarray(): XArray_uint8 {
        return XArray_uint8.wrap(this.vprValue.at("f_xarray"));
    }

    set f_xarray(value: XArray_uint8) {
        this.vprValue.set("f_xarray", value.vprValue);
    }
    get f_xarray_s(): XArray_Test_StructureS {
        return XArray_Test_StructureS.wrap(this.vprValue.at("f_xarray_s"));
    }

    set f_xarray_s(value: XArray_Test_StructureS) {
        this.vprValue.set("f_xarray_s", value.vprValue);
    }
    get f_map_vs(): Map_Vector_Test_StructureS_to_string {
        return Map_Vector_Test_StructureS_to_string.wrap(this.vprValue.at("f_map_vs"));
    }

    set f_map_vs(value: Map_Vector_Test_StructureS_to_string) {
        this.vprValue.set("f_map_vs", value.vprValue);
    }
    get f_variant(): Variant_string_uint8_Test_StructureS {
        return Variant_string_uint8_Test_StructureS.wrap(this.vprValue.at("f_variant"));
    }

    set f_variant(value: Variant_string_uint8_Test_StructureS) {
        this.vprValue.set("f_variant", value.vprValue);
    }
    get f_any(): dsviper.ValueAny {
        return this.vprValue.at("f_any") as dsviper.ValueAny;
    }

    set f_any(value: dsviper.ValueAny) {
        this.vprValue.set("f_any", value);
    }
    get f_E(): Test_EnumerationE {
        return Test_EnumerationE.wrap(this.vprValue.at("f_E"));
    }

    set f_E(value: Test_EnumerationE) {
        this.vprValue.set("f_E", value.vprValue);
    }
    get f_S(): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.at("f_S"));
    }

    set f_S(value: Test_StructureS) {
        this.vprValue.set("f_S", value.vprValue);
    }
    get f_T(): Test_StructureT {
        return Test_StructureT.wrap(this.vprValue.at("f_T"));
    }

    set f_T(value: Test_StructureT) {
        this.vprValue.set("f_T", value.vprValue);
    }
    get f_A(): Test_ConceptAKey {
        return Test_ConceptAKey.wrap(this.vprValue.at("f_A"));
    }

    set f_A(value: Test_ConceptAKey) {
        this.vprValue.set("f_A", value.vprValue);
    }
    get f_B(): Test_ConceptBKey {
        return Test_ConceptBKey.wrap(this.vprValue.at("f_B"));
    }

    set f_B(value: Test_ConceptBKey) {
        this.vprValue.set("f_B", value.vprValue);
    }
    get f_C(): Test_ConceptCKey {
        return Test_ConceptCKey.wrap(this.vprValue.at("f_C"));
    }

    set f_C(value: Test_ConceptCKey) {
        this.vprValue.set("f_C", value.vprValue);
    }
    get f_D(): Test_ConceptDKey {
        return Test_ConceptDKey.wrap(this.vprValue.at("f_D"));
    }

    set f_D(value: Test_ConceptDKey) {
        this.vprValue.set("f_D", value.vprValue);
    }
    get f_Klub(): Test_KlubKey {
        return Test_KlubKey.wrap(this.vprValue.at("f_Klub"));
    }

    set f_Klub(value: Test_KlubKey) {
        this.vprValue.set("f_Klub", value.vprValue);
    }
    get f_any_concept(): AnyConceptKey {
        return AnyConceptKey.wrap(this.vprValue.at("f_any_concept"));
    }

    set f_any_concept(value: AnyConceptKey) {
        this.vprValue.set("f_any_concept", value.vprValue);
    }

    copy(): Test_StructureU {
        return new Test_StructureU(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_StructureU {
        const value = dsviper.ValueStructure.cast(dsviper.Value.decode(blob, mt.type_Test_StructureU(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_StructureU(value);
    }
}

// struct Test::StructureV
export class Test_StructureV extends Proxy<dsviper.ValueStructure> {

    constructor(value?: dsviper.ValueStructure | Record<string, dsviper.InputValue>) {
        if (value instanceof dsviper.ValueStructure) {
            if (!value.type().equals(mt.type_Test_StructureV())) {
                throw new TypeError("value is not a Test::StructureV");
            }
            super(value);
        } else {
            super(new dsviper.ValueStructure(mt.type_Test_StructureV(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Test_StructureV {
        return new Test_StructureV(dsviper.ValueStructure.cast(value as dsviper.Value));
    }

    get f_bool(): boolean {
        return this.vprValue.at("f_bool") as boolean;
    }

    set f_bool(value: boolean) {
        this.vprValue.set("f_bool", value);
    }
    get f_uint8(): number {
        return this.vprValue.at("f_uint8") as number;
    }

    set f_uint8(value: number) {
        this.vprValue.set("f_uint8", value);
    }
    get f_uint16(): number {
        return this.vprValue.at("f_uint16") as number;
    }

    set f_uint16(value: number) {
        this.vprValue.set("f_uint16", value);
    }
    get f_uint32(): number {
        return this.vprValue.at("f_uint32") as number;
    }

    set f_uint32(value: number) {
        this.vprValue.set("f_uint32", value);
    }
    get f_uint64(): bigint {
        return this.vprValue.at("f_uint64") as bigint;
    }

    set f_uint64(value: bigint) {
        this.vprValue.set("f_uint64", value);
    }
    get f_int8(): number {
        return this.vprValue.at("f_int8") as number;
    }

    set f_int8(value: number) {
        this.vprValue.set("f_int8", value);
    }
    get f_int16(): number {
        return this.vprValue.at("f_int16") as number;
    }

    set f_int16(value: number) {
        this.vprValue.set("f_int16", value);
    }
    get f_int32(): number {
        return this.vprValue.at("f_int32") as number;
    }

    set f_int32(value: number) {
        this.vprValue.set("f_int32", value);
    }
    get f_int64(): bigint {
        return this.vprValue.at("f_int64") as bigint;
    }

    set f_int64(value: bigint) {
        this.vprValue.set("f_int64", value);
    }
    get f_float(): number {
        return this.vprValue.at("f_float") as number;
    }

    set f_float(value: number) {
        this.vprValue.set("f_float", value);
    }
    get f_double(): number {
        return this.vprValue.at("f_double") as number;
    }

    set f_double(value: number) {
        this.vprValue.set("f_double", value);
    }
    get f_uuid(): dsviper.ValueUUId {
        return this.vprValue.at("f_uuid") as dsviper.ValueUUId;
    }

    set f_uuid(value: dsviper.ValueUUId) {
        this.vprValue.set("f_uuid", value);
    }
    get f_string(): string {
        return this.vprValue.at("f_string") as string;
    }

    set f_string(value: string) {
        this.vprValue.set("f_string", value);
    }
    get f_vec(): Vec_uint8_2 {
        return Vec_uint8_2.wrap(this.vprValue.at("f_vec"));
    }

    set f_vec(value: Vec_uint8_2) {
        this.vprValue.set("f_vec", value.vprValue);
    }
    get f_mat(): Mat_uint8_2_3 {
        return Mat_uint8_2_3.wrap(this.vprValue.at("f_mat"));
    }

    set f_mat(value: Mat_uint8_2_3) {
        this.vprValue.set("f_mat", value.vprValue);
    }
    get f_tuple(): Tuple_uint8_string {
        return Tuple_uint8_string.wrap(this.vprValue.at("f_tuple"));
    }

    set f_tuple(value: Tuple_uint8_string) {
        this.vprValue.set("f_tuple", value.vprValue);
    }
    get f_optional(): Optional_uint8 {
        return Optional_uint8.wrap(this.vprValue.at("f_optional"));
    }

    set f_optional(value: Optional_uint8) {
        this.vprValue.set("f_optional", value.vprValue);
    }
    get f_vector(): Vector_uint8 {
        return Vector_uint8.wrap(this.vprValue.at("f_vector"));
    }

    set f_vector(value: Vector_uint8) {
        this.vprValue.set("f_vector", value.vprValue);
    }
    get f_set(): Set_uint8 {
        return Set_uint8.wrap(this.vprValue.at("f_set"));
    }

    set f_set(value: Set_uint8) {
        this.vprValue.set("f_set", value.vprValue);
    }
    get f_map(): Map_uint8_to_string {
        return Map_uint8_to_string.wrap(this.vprValue.at("f_map"));
    }

    set f_map(value: Map_uint8_to_string) {
        this.vprValue.set("f_map", value.vprValue);
    }
    get f_E(): Test_EnumerationE {
        return Test_EnumerationE.wrap(this.vprValue.at("f_E"));
    }

    set f_E(value: Test_EnumerationE) {
        this.vprValue.set("f_E", value.vprValue);
    }
    get f_S(): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.at("f_S"));
    }

    set f_S(value: Test_StructureS) {
        this.vprValue.set("f_S", value.vprValue);
    }
    get f_T(): Test_StructureT {
        return Test_StructureT.wrap(this.vprValue.at("f_T"));
    }

    set f_T(value: Test_StructureT) {
        this.vprValue.set("f_T", value.vprValue);
    }

    copy(): Test_StructureV {
        return new Test_StructureV(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_StructureV {
        const value = dsviper.ValueStructure.cast(dsviper.Value.decode(blob, mt.type_Test_StructureV(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_StructureV(value);
    }
}

// struct Test::StructureW
export class Test_StructureW extends Proxy<dsviper.ValueStructure> {

    constructor(value?: dsviper.ValueStructure | Record<string, dsviper.InputValue>) {
        if (value instanceof dsviper.ValueStructure) {
            if (!value.type().equals(mt.type_Test_StructureW())) {
                throw new TypeError("value is not a Test::StructureW");
            }
            super(value);
        } else {
            super(new dsviper.ValueStructure(mt.type_Test_StructureW(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Test_StructureW {
        return new Test_StructureW(dsviper.ValueStructure.cast(value as dsviper.Value));
    }

    get f_single(): number {
        return this.vprValue.at("f_single") as number;
    }

    set f_single(value: number) {
        this.vprValue.set("f_single", value);
    }

    copy(): Test_StructureW {
        return new Test_StructureW(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Test_StructureW {
        const value = dsviper.ValueStructure.cast(dsviper.Value.decode(blob, mt.type_Test_StructureW(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Test_StructureW(value);
    }
}

// vec<uint8, 2>
export class Vec_uint8_2 extends Proxy<dsviper.ValueVec> {

    constructor(value?: dsviper.ValueVec | number[]) {
        if (value instanceof dsviper.ValueVec) {
            if (!value.type().equals(mt.type_vec2_uint8())) {
                throw new TypeError("value is not a vec<uint8, 2>");
            }
            super(value);
        } else {
            super(new dsviper.ValueVec(mt.type_vec2_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Vec_uint8_2 {
        return new Vec_uint8_2(dsviper.ValueVec.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): number {
        return this.vprValue.at(index) as number;
    }

    set(index: number, value: number): void {
        this.vprValue.set(index, value);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v as number;
        }
    }

    toArray(): number[] {
        return this.vprValue.toArray() as number[];
    }

    copy(): Vec_uint8_2 {
        return new Vec_uint8_2(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Vec_uint8_2 {
        const value = dsviper.ValueVec.cast(dsviper.Value.decode(blob, mt.type_vec2_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Vec_uint8_2(value);
    }
}

// mat<uint8, 2, 2>
export class Mat_uint8_2_2 extends Proxy<dsviper.ValueMat> {

    constructor(value?: dsviper.ValueMat | number[][]) {
        if (value instanceof dsviper.ValueMat) {
            if (!value.type().equals(mt.type_mat2x2_uint8())) {
                throw new TypeError("value is not a mat<uint8, 2, 2>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMat(mt.type_mat2x2_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Mat_uint8_2_2 {
        return new Mat_uint8_2_2(dsviper.ValueMat.cast(value as dsviper.Value));
    }

    get columns(): number {
        return this.vprValue.columns();
    }

    get rows(): number {
        return this.vprValue.rows();
    }

    get size(): number {
        return this.vprValue.columns() * this.vprValue.rows();
    }

    at(row: number, column: number): number {
        return this.vprValue.at(row, column) as number;
    }

    set(row: number, column: number, value: number): void {
        this.vprValue.set(row, column, value);
    }

    row(row: number): number[] {
        return (this.vprValue.toArray() as number[][])[row];
    }

    setRow(row: number, values: number[]): void {
        values.forEach((value, column) => this.vprValue.set(row, column, value));
    }

    toArray(): number[][] {
        return this.vprValue.toArray() as number[][];
    }

    copy(): Mat_uint8_2_2 {
        return new Mat_uint8_2_2(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Mat_uint8_2_2 {
        const value = dsviper.ValueMat.cast(dsviper.Value.decode(blob, mt.type_mat2x2_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Mat_uint8_2_2(value);
    }
}

// mat<uint8, 2, 3>
export class Mat_uint8_2_3 extends Proxy<dsviper.ValueMat> {

    constructor(value?: dsviper.ValueMat | number[][]) {
        if (value instanceof dsviper.ValueMat) {
            if (!value.type().equals(mt.type_mat2x3_uint8())) {
                throw new TypeError("value is not a mat<uint8, 2, 3>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMat(mt.type_mat2x3_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Mat_uint8_2_3 {
        return new Mat_uint8_2_3(dsviper.ValueMat.cast(value as dsviper.Value));
    }

    get columns(): number {
        return this.vprValue.columns();
    }

    get rows(): number {
        return this.vprValue.rows();
    }

    get size(): number {
        return this.vprValue.columns() * this.vprValue.rows();
    }

    at(row: number, column: number): number {
        return this.vprValue.at(row, column) as number;
    }

    set(row: number, column: number, value: number): void {
        this.vprValue.set(row, column, value);
    }

    row(row: number): number[] {
        return (this.vprValue.toArray() as number[][])[row];
    }

    setRow(row: number, values: number[]): void {
        values.forEach((value, column) => this.vprValue.set(row, column, value));
    }

    toArray(): number[][] {
        return this.vprValue.toArray() as number[][];
    }

    copy(): Mat_uint8_2_3 {
        return new Mat_uint8_2_3(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Mat_uint8_2_3 {
        const value = dsviper.ValueMat.cast(dsviper.Value.decode(blob, mt.type_mat2x3_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Mat_uint8_2_3(value);
    }
}

// tuple<uint8, string>
export class Tuple_uint8_string extends Proxy<dsviper.ValueTuple> {

    constructor(value?: dsviper.ValueTuple | dsviper.InputValue[]) {
        if (value instanceof dsviper.ValueTuple) {
            if (!value.type().equals(mt.type_tuple_uint8_string())) {
                throw new TypeError("value is not a tuple<uint8, string>");
            }
            super(value);
        } else {
            super(new dsviper.ValueTuple(mt.type_tuple_uint8_string(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Tuple_uint8_string {
        return new Tuple_uint8_string(dsviper.ValueTuple.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): dsviper.OutputValue {
        return this.vprValue.at(index);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v;
        }
    }

    copy(): Tuple_uint8_string {
        return new Tuple_uint8_string(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Tuple_uint8_string {
        const value = dsviper.ValueTuple.cast(dsviper.Value.decode(blob, mt.type_tuple_uint8_string(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Tuple_uint8_string(value);
    }
}

// optional<uint8>
export class Optional_uint8 extends Proxy<dsviper.ValueOptional> {

    constructor(value?: dsviper.ValueOptional | number | null) {
        if (value instanceof dsviper.ValueOptional) {
            if (!value.type().equals(mt.type_optional_uint8())) {
                throw new TypeError("value is not a optional<uint8>");
            }
            super(value);
        } else if (value === null || value === undefined) {
            super(new dsviper.ValueOptional(mt.type_optional_uint8()));
        } else {
            super(new dsviper.ValueOptional(mt.type_optional_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Optional_uint8 {
        return new Optional_uint8(dsviper.ValueOptional.cast(value as dsviper.Value));
    }

    isNil(): boolean {
        return this.vprValue.isNil();
    }

    unwrap(): number {
        return this.vprValue.unwrap() as number;
    }

    wrap(value: number): void {
        this.vprValue.wrap(value);
    }

    get(defaultValue?: number): number {
        return this.vprValue.get(defaultValue === undefined ? undefined : defaultValue) as number;
    }

    copy(): Optional_uint8 {
        return new Optional_uint8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Optional_uint8 {
        const value = dsviper.ValueOptional.cast(dsviper.Value.decode(blob, mt.type_optional_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Optional_uint8(value);
    }
}

// vector<Test::StructureS>
export class Vector_Test_StructureS extends Proxy<dsviper.ValueVector> {

    constructor(value?: dsviper.ValueVector | dsviper.InputValue) {
        if (value instanceof dsviper.ValueVector) {
            if (!value.type().equals(mt.type_vector_Test_StructureS())) {
                throw new TypeError("value is not a vector<Test::StructureS>");
            }
            super(value);
        } else {
            super(new dsviper.ValueVector(mt.type_vector_Test_StructureS(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Vector_Test_StructureS {
        return new Vector_Test_StructureS(dsviper.ValueVector.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.at(index));
    }

    set(index: number, value: Test_StructureS): void {
        this.vprValue.set(index, value.vprValue);
    }

    append(value: Test_StructureS): void {
        this.vprValue.append(value.vprValue);
    }

    insert(index: number, value: Test_StructureS): void {
        this.vprValue.insert(index, value.vprValue);
    }

    pop(index?: number): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.pop(index));
    }

    remove(value: Test_StructureS): void {
        this.vprValue.remove(value.vprValue);
    }

    contains(value: Test_StructureS): boolean {
        return this.vprValue.contains(value.vprValue);
    }

    count(value: Test_StructureS): number {
        return this.vprValue.count(value.vprValue);
    }

    index(value: Test_StructureS): number {
        return this.vprValue.index(value.vprValue);
    }

    extend(other: Vector_Test_StructureS): void {
        this.vprValue.extend(other.vprValue);
    }

    concat(other: Vector_Test_StructureS): Vector_Test_StructureS {
        const result = new Vector_Test_StructureS(this.vprValue.copy());
        result.extend(other);
        return result;
    }

    clear(): void {
        this.vprValue.clear();
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield Test_StructureS.wrap(v);
        }
    }

    copy(): Vector_Test_StructureS {
        return new Vector_Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Vector_Test_StructureS {
        const value = dsviper.ValueVector.cast(dsviper.Value.decode(blob, mt.type_vector_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Vector_Test_StructureS(value);
    }
}

// vector<uint8>
export class Vector_uint8 extends Proxy<dsviper.ValueVector> {

    constructor(value?: dsviper.ValueVector | dsviper.InputValue) {
        if (value instanceof dsviper.ValueVector) {
            if (!value.type().equals(mt.type_vector_uint8())) {
                throw new TypeError("value is not a vector<uint8>");
            }
            super(value);
        } else {
            super(new dsviper.ValueVector(mt.type_vector_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Vector_uint8 {
        return new Vector_uint8(dsviper.ValueVector.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): number {
        return this.vprValue.at(index) as number;
    }

    set(index: number, value: number): void {
        this.vprValue.set(index, value);
    }

    append(value: number): void {
        this.vprValue.append(value);
    }

    insert(index: number, value: number): void {
        this.vprValue.insert(index, value);
    }

    pop(index?: number): number {
        return this.vprValue.pop(index) as number;
    }

    remove(value: number): void {
        this.vprValue.remove(value);
    }

    contains(value: number): boolean {
        return this.vprValue.contains(value);
    }

    count(value: number): number {
        return this.vprValue.count(value);
    }

    index(value: number): number {
        return this.vprValue.index(value);
    }

    extend(other: Vector_uint8): void {
        this.vprValue.extend(other.vprValue);
    }

    concat(other: Vector_uint8): Vector_uint8 {
        const result = new Vector_uint8(this.vprValue.copy());
        result.extend(other);
        return result;
    }

    clear(): void {
        this.vprValue.clear();
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v as number;
        }
    }

    copy(): Vector_uint8 {
        return new Vector_uint8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Vector_uint8 {
        const value = dsviper.ValueVector.cast(dsviper.Value.decode(blob, mt.type_vector_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Vector_uint8(value);
    }
}

// set<Test::StructureS>
export class Set_Test_StructureS extends Proxy<dsviper.ValueSet> {

    constructor(value?: dsviper.ValueSet | dsviper.InputValue) {
        if (value instanceof dsviper.ValueSet) {
            if (!value.type().equals(mt.type_set_Test_StructureS())) {
                throw new TypeError("value is not a set<Test::StructureS>");
            }
            super(value);
        } else {
            super(new dsviper.ValueSet(mt.type_set_Test_StructureS(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Set_Test_StructureS {
        return new Set_Test_StructureS(dsviper.ValueSet.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.at(index));
    }

    add(value: Test_StructureS): void {
        this.vprValue.add(value.vprValue);
    }

    discard(value: Test_StructureS): void {
        this.vprValue.discard(value.vprValue);
    }

    remove(value: Test_StructureS): void {
        this.vprValue.remove(value.vprValue);
    }

    pop(): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.pop());
    }

    contains(value: Test_StructureS): boolean {
        return this.vprValue.index(value.vprValue) !== undefined;
    }

    clear(): void {
        this.vprValue.clear();
    }

    min(): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.min());
    }

    max(): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.max());
    }

    union(other: Set_Test_StructureS): Set_Test_StructureS {
        return new Set_Test_StructureS(this.vprValue.union(other.vprValue));
    }

    intersection(other: Set_Test_StructureS): Set_Test_StructureS {
        return new Set_Test_StructureS(this.vprValue.intersection(other.vprValue));
    }

    difference(other: Set_Test_StructureS): Set_Test_StructureS {
        return new Set_Test_StructureS(this.vprValue.difference(other.vprValue));
    }

    symmetricDifference(other: Set_Test_StructureS): Set_Test_StructureS {
        return new Set_Test_StructureS(this.vprValue.symmetricDifference(other.vprValue));
    }

    update(other: Set_Test_StructureS): void {
        this.vprValue.update(other.vprValue);
    }

    intersectionUpdate(other: Set_Test_StructureS): void {
        this.vprValue.intersectionUpdate(other.vprValue);
    }

    differenceUpdate(other: Set_Test_StructureS): void {
        this.vprValue.differenceUpdate(other.vprValue);
    }

    symmetricDifferenceUpdate(other: Set_Test_StructureS): void {
        this.vprValue.symmetricDifferenceUpdate(other.vprValue);
    }

    isdisjoint(other: Set_Test_StructureS): boolean {
        return this.vprValue.isdisjoint(other.vprValue);
    }

    issubset(other: Set_Test_StructureS): boolean {
        return this.vprValue.issubset(other.vprValue);
    }

    issuperset(other: Set_Test_StructureS): boolean {
        return this.vprValue.issuperset(other.vprValue);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield Test_StructureS.wrap(v);
        }
    }

    copy(): Set_Test_StructureS {
        return new Set_Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Set_Test_StructureS {
        const value = dsviper.ValueSet.cast(dsviper.Value.decode(blob, mt.type_set_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Set_Test_StructureS(value);
    }
}

// set<int8>
export class Set_int8 extends Proxy<dsviper.ValueSet> {

    constructor(value?: dsviper.ValueSet | dsviper.InputValue) {
        if (value instanceof dsviper.ValueSet) {
            if (!value.type().equals(mt.type_set_int8())) {
                throw new TypeError("value is not a set<int8>");
            }
            super(value);
        } else {
            super(new dsviper.ValueSet(mt.type_set_int8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Set_int8 {
        return new Set_int8(dsviper.ValueSet.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): number {
        return this.vprValue.at(index) as number;
    }

    add(value: number): void {
        this.vprValue.add(value);
    }

    discard(value: number): void {
        this.vprValue.discard(value);
    }

    remove(value: number): void {
        this.vprValue.remove(value);
    }

    pop(): number {
        return this.vprValue.pop() as number;
    }

    contains(value: number): boolean {
        return this.vprValue.index(value) !== undefined;
    }

    clear(): void {
        this.vprValue.clear();
    }

    min(): number {
        return this.vprValue.min() as number;
    }

    max(): number {
        return this.vprValue.max() as number;
    }

    union(other: Set_int8): Set_int8 {
        return new Set_int8(this.vprValue.union(other.vprValue));
    }

    intersection(other: Set_int8): Set_int8 {
        return new Set_int8(this.vprValue.intersection(other.vprValue));
    }

    difference(other: Set_int8): Set_int8 {
        return new Set_int8(this.vprValue.difference(other.vprValue));
    }

    symmetricDifference(other: Set_int8): Set_int8 {
        return new Set_int8(this.vprValue.symmetricDifference(other.vprValue));
    }

    update(other: Set_int8): void {
        this.vprValue.update(other.vprValue);
    }

    intersectionUpdate(other: Set_int8): void {
        this.vprValue.intersectionUpdate(other.vprValue);
    }

    differenceUpdate(other: Set_int8): void {
        this.vprValue.differenceUpdate(other.vprValue);
    }

    symmetricDifferenceUpdate(other: Set_int8): void {
        this.vprValue.symmetricDifferenceUpdate(other.vprValue);
    }

    isdisjoint(other: Set_int8): boolean {
        return this.vprValue.isdisjoint(other.vprValue);
    }

    issubset(other: Set_int8): boolean {
        return this.vprValue.issubset(other.vprValue);
    }

    issuperset(other: Set_int8): boolean {
        return this.vprValue.issuperset(other.vprValue);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v as number;
        }
    }

    copy(): Set_int8 {
        return new Set_int8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Set_int8 {
        const value = dsviper.ValueSet.cast(dsviper.Value.decode(blob, mt.type_set_int8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Set_int8(value);
    }
}

// set<string>
export class Set_string extends Proxy<dsviper.ValueSet> {

    constructor(value?: dsviper.ValueSet | dsviper.InputValue) {
        if (value instanceof dsviper.ValueSet) {
            if (!value.type().equals(mt.type_set_string())) {
                throw new TypeError("value is not a set<string>");
            }
            super(value);
        } else {
            super(new dsviper.ValueSet(mt.type_set_string(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Set_string {
        return new Set_string(dsviper.ValueSet.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): string {
        return this.vprValue.at(index) as string;
    }

    add(value: string): void {
        this.vprValue.add(value);
    }

    discard(value: string): void {
        this.vprValue.discard(value);
    }

    remove(value: string): void {
        this.vprValue.remove(value);
    }

    pop(): string {
        return this.vprValue.pop() as string;
    }

    contains(value: string): boolean {
        return this.vprValue.index(value) !== undefined;
    }

    clear(): void {
        this.vprValue.clear();
    }

    min(): string {
        return this.vprValue.min() as string;
    }

    max(): string {
        return this.vprValue.max() as string;
    }

    union(other: Set_string): Set_string {
        return new Set_string(this.vprValue.union(other.vprValue));
    }

    intersection(other: Set_string): Set_string {
        return new Set_string(this.vprValue.intersection(other.vprValue));
    }

    difference(other: Set_string): Set_string {
        return new Set_string(this.vprValue.difference(other.vprValue));
    }

    symmetricDifference(other: Set_string): Set_string {
        return new Set_string(this.vprValue.symmetricDifference(other.vprValue));
    }

    update(other: Set_string): void {
        this.vprValue.update(other.vprValue);
    }

    intersectionUpdate(other: Set_string): void {
        this.vprValue.intersectionUpdate(other.vprValue);
    }

    differenceUpdate(other: Set_string): void {
        this.vprValue.differenceUpdate(other.vprValue);
    }

    symmetricDifferenceUpdate(other: Set_string): void {
        this.vprValue.symmetricDifferenceUpdate(other.vprValue);
    }

    isdisjoint(other: Set_string): boolean {
        return this.vprValue.isdisjoint(other.vprValue);
    }

    issubset(other: Set_string): boolean {
        return this.vprValue.issubset(other.vprValue);
    }

    issuperset(other: Set_string): boolean {
        return this.vprValue.issuperset(other.vprValue);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v as string;
        }
    }

    copy(): Set_string {
        return new Set_string(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Set_string {
        const value = dsviper.ValueSet.cast(dsviper.Value.decode(blob, mt.type_set_string(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Set_string(value);
    }
}

// set<uint8>
export class Set_uint8 extends Proxy<dsviper.ValueSet> {

    constructor(value?: dsviper.ValueSet | dsviper.InputValue) {
        if (value instanceof dsviper.ValueSet) {
            if (!value.type().equals(mt.type_set_uint8())) {
                throw new TypeError("value is not a set<uint8>");
            }
            super(value);
        } else {
            super(new dsviper.ValueSet(mt.type_set_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Set_uint8 {
        return new Set_uint8(dsviper.ValueSet.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): number {
        return this.vprValue.at(index) as number;
    }

    add(value: number): void {
        this.vprValue.add(value);
    }

    discard(value: number): void {
        this.vprValue.discard(value);
    }

    remove(value: number): void {
        this.vprValue.remove(value);
    }

    pop(): number {
        return this.vprValue.pop() as number;
    }

    contains(value: number): boolean {
        return this.vprValue.index(value) !== undefined;
    }

    clear(): void {
        this.vprValue.clear();
    }

    min(): number {
        return this.vprValue.min() as number;
    }

    max(): number {
        return this.vprValue.max() as number;
    }

    union(other: Set_uint8): Set_uint8 {
        return new Set_uint8(this.vprValue.union(other.vprValue));
    }

    intersection(other: Set_uint8): Set_uint8 {
        return new Set_uint8(this.vprValue.intersection(other.vprValue));
    }

    difference(other: Set_uint8): Set_uint8 {
        return new Set_uint8(this.vprValue.difference(other.vprValue));
    }

    symmetricDifference(other: Set_uint8): Set_uint8 {
        return new Set_uint8(this.vprValue.symmetricDifference(other.vprValue));
    }

    update(other: Set_uint8): void {
        this.vprValue.update(other.vprValue);
    }

    intersectionUpdate(other: Set_uint8): void {
        this.vprValue.intersectionUpdate(other.vprValue);
    }

    differenceUpdate(other: Set_uint8): void {
        this.vprValue.differenceUpdate(other.vprValue);
    }

    symmetricDifferenceUpdate(other: Set_uint8): void {
        this.vprValue.symmetricDifferenceUpdate(other.vprValue);
    }

    isdisjoint(other: Set_uint8): boolean {
        return this.vprValue.isdisjoint(other.vprValue);
    }

    issubset(other: Set_uint8): boolean {
        return this.vprValue.issubset(other.vprValue);
    }

    issuperset(other: Set_uint8): boolean {
        return this.vprValue.issuperset(other.vprValue);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v as number;
        }
    }

    copy(): Set_uint8 {
        return new Set_uint8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Set_uint8 {
        const value = dsviper.ValueSet.cast(dsviper.Value.decode(blob, mt.type_set_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Set_uint8(value);
    }
}

// set<vector<Test::StructureS>>
export class Set_Vector_Test_StructureS extends Proxy<dsviper.ValueSet> {

    constructor(value?: dsviper.ValueSet | dsviper.InputValue) {
        if (value instanceof dsviper.ValueSet) {
            if (!value.type().equals(mt.type_set_vector_Test_StructureS())) {
                throw new TypeError("value is not a set<vector<Test::StructureS>>");
            }
            super(value);
        } else {
            super(new dsviper.ValueSet(mt.type_set_vector_Test_StructureS(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Set_Vector_Test_StructureS {
        return new Set_Vector_Test_StructureS(dsviper.ValueSet.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): Vector_Test_StructureS {
        return Vector_Test_StructureS.wrap(this.vprValue.at(index));
    }

    add(value: Vector_Test_StructureS): void {
        this.vprValue.add(value.vprValue);
    }

    discard(value: Vector_Test_StructureS): void {
        this.vprValue.discard(value.vprValue);
    }

    remove(value: Vector_Test_StructureS): void {
        this.vprValue.remove(value.vprValue);
    }

    pop(): Vector_Test_StructureS {
        return Vector_Test_StructureS.wrap(this.vprValue.pop());
    }

    contains(value: Vector_Test_StructureS): boolean {
        return this.vprValue.index(value.vprValue) !== undefined;
    }

    clear(): void {
        this.vprValue.clear();
    }

    min(): Vector_Test_StructureS {
        return Vector_Test_StructureS.wrap(this.vprValue.min());
    }

    max(): Vector_Test_StructureS {
        return Vector_Test_StructureS.wrap(this.vprValue.max());
    }

    union(other: Set_Vector_Test_StructureS): Set_Vector_Test_StructureS {
        return new Set_Vector_Test_StructureS(this.vprValue.union(other.vprValue));
    }

    intersection(other: Set_Vector_Test_StructureS): Set_Vector_Test_StructureS {
        return new Set_Vector_Test_StructureS(this.vprValue.intersection(other.vprValue));
    }

    difference(other: Set_Vector_Test_StructureS): Set_Vector_Test_StructureS {
        return new Set_Vector_Test_StructureS(this.vprValue.difference(other.vprValue));
    }

    symmetricDifference(other: Set_Vector_Test_StructureS): Set_Vector_Test_StructureS {
        return new Set_Vector_Test_StructureS(this.vprValue.symmetricDifference(other.vprValue));
    }

    update(other: Set_Vector_Test_StructureS): void {
        this.vprValue.update(other.vprValue);
    }

    intersectionUpdate(other: Set_Vector_Test_StructureS): void {
        this.vprValue.intersectionUpdate(other.vprValue);
    }

    differenceUpdate(other: Set_Vector_Test_StructureS): void {
        this.vprValue.differenceUpdate(other.vprValue);
    }

    symmetricDifferenceUpdate(other: Set_Vector_Test_StructureS): void {
        this.vprValue.symmetricDifferenceUpdate(other.vprValue);
    }

    isdisjoint(other: Set_Vector_Test_StructureS): boolean {
        return this.vprValue.isdisjoint(other.vprValue);
    }

    issubset(other: Set_Vector_Test_StructureS): boolean {
        return this.vprValue.issubset(other.vprValue);
    }

    issuperset(other: Set_Vector_Test_StructureS): boolean {
        return this.vprValue.issuperset(other.vprValue);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield Vector_Test_StructureS.wrap(v);
        }
    }

    copy(): Set_Vector_Test_StructureS {
        return new Set_Vector_Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Set_Vector_Test_StructureS {
        const value = dsviper.ValueSet.cast(dsviper.Value.decode(blob, mt.type_set_vector_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Set_Vector_Test_StructureS(value);
    }
}

// map<Test::StructureS, string>
export class Map_Test_StructureS_to_string extends Proxy<dsviper.ValueMap> {

    constructor(value?: dsviper.ValueMap | dsviper.InputValue) {
        if (value instanceof dsviper.ValueMap) {
            if (!value.type().equals(mt.type_map_Test_StructureS_to_string())) {
                throw new TypeError("value is not a map<Test::StructureS, string>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMap(mt.type_map_Test_StructureS_to_string(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Map_Test_StructureS_to_string {
        return new Map_Test_StructureS_to_string(dsviper.ValueMap.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(key: Test_StructureS): string {
        return this.vprValue.at(key.vprValue) as string;
    }

    set(key: Test_StructureS, value: string): void {
        this.vprValue.set(key.vprValue, value);
    }

    has(key: Test_StructureS): boolean {
        return this.vprValue.contains(key.vprValue);
    }

    get(key: Test_StructureS, defaultValue?: string): string | undefined {
        const value = this.vprValue.get(key.vprValue, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    remove(key: Test_StructureS): void {
        this.vprValue.remove(key.vprValue);
    }

    pop(key: Test_StructureS, defaultValue?: string): string | undefined {
        const value = this.vprValue.pop(key.vprValue, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    popitem(): [Test_StructureS, string] {
        const entry = this.vprValue.popitem() as unknown as dsviper.OutputValue[];
        return [Test_StructureS.wrap(entry[0]), entry[1] as string];
    }

    setdefault(key: Test_StructureS, defaultValue: string): void {
        this.vprValue.setdefault(key.vprValue, defaultValue);
    }

    update(other: Map_Test_StructureS_to_string): void {
        this.vprValue.update(other.vprValue);
    }

    clear(): void {
        this.vprValue.clear();
    }

    *keys() {
        for (const [k] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield Test_StructureS.wrap(k);
        }
    }

    *values() {
        for (const [, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield value as string;
        }
    }

    *entries(): Generator<[Test_StructureS, string]> {
        for (const [k, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield [Test_StructureS.wrap(k), value as string];
        }
    }

    items(): [Test_StructureS, string][] {
        return [...this.entries()];
    }

    *[Symbol.iterator]() {
        yield* this.keys();
    }

    copy(): Map_Test_StructureS_to_string {
        return new Map_Test_StructureS_to_string(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Map_Test_StructureS_to_string {
        const value = dsviper.ValueMap.cast(dsviper.Value.decode(blob, mt.type_map_Test_StructureS_to_string(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Map_Test_StructureS_to_string(value);
    }
}

// map<int8, string>
export class Map_int8_to_string extends Proxy<dsviper.ValueMap> {

    constructor(value?: dsviper.ValueMap | dsviper.InputValue) {
        if (value instanceof dsviper.ValueMap) {
            if (!value.type().equals(mt.type_map_int8_to_string())) {
                throw new TypeError("value is not a map<int8, string>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMap(mt.type_map_int8_to_string(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Map_int8_to_string {
        return new Map_int8_to_string(dsviper.ValueMap.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(key: number): string {
        return this.vprValue.at(key) as string;
    }

    set(key: number, value: string): void {
        this.vprValue.set(key, value);
    }

    has(key: number): boolean {
        return this.vprValue.contains(key);
    }

    get(key: number, defaultValue?: string): string | undefined {
        const value = this.vprValue.get(key, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    remove(key: number): void {
        this.vprValue.remove(key);
    }

    pop(key: number, defaultValue?: string): string | undefined {
        const value = this.vprValue.pop(key, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    popitem(): [number, string] {
        const entry = this.vprValue.popitem() as unknown as dsviper.OutputValue[];
        return [entry[0] as number, entry[1] as string];
    }

    setdefault(key: number, defaultValue: string): void {
        this.vprValue.setdefault(key, defaultValue);
    }

    update(other: Map_int8_to_string): void {
        this.vprValue.update(other.vprValue);
    }

    clear(): void {
        this.vprValue.clear();
    }

    *keys() {
        for (const [k] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield k as number;
        }
    }

    *values() {
        for (const [, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield value as string;
        }
    }

    *entries(): Generator<[number, string]> {
        for (const [k, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield [k as number, value as string];
        }
    }

    items(): [number, string][] {
        return [...this.entries()];
    }

    *[Symbol.iterator]() {
        yield* this.keys();
    }

    copy(): Map_int8_to_string {
        return new Map_int8_to_string(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Map_int8_to_string {
        const value = dsviper.ValueMap.cast(dsviper.Value.decode(blob, mt.type_map_int8_to_string(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Map_int8_to_string(value);
    }
}

// map<string, Test::StructureS>
export class Map_string_to_Test_StructureS extends Proxy<dsviper.ValueMap> {

    constructor(value?: dsviper.ValueMap | dsviper.InputValue) {
        if (value instanceof dsviper.ValueMap) {
            if (!value.type().equals(mt.type_map_string_to_Test_StructureS())) {
                throw new TypeError("value is not a map<string, Test::StructureS>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMap(mt.type_map_string_to_Test_StructureS(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Map_string_to_Test_StructureS {
        return new Map_string_to_Test_StructureS(dsviper.ValueMap.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(key: string): Test_StructureS {
        return Test_StructureS.wrap(this.vprValue.at(key));
    }

    set(key: string, value: Test_StructureS): void {
        this.vprValue.set(key, value.vprValue);
    }

    has(key: string): boolean {
        return this.vprValue.contains(key);
    }

    get(key: string, defaultValue?: Test_StructureS): Test_StructureS | undefined {
        const value = this.vprValue.get(key, defaultValue === undefined ? undefined : defaultValue.vprValue);
        return value === undefined ? undefined : Test_StructureS.wrap(value);
    }

    remove(key: string): void {
        this.vprValue.remove(key);
    }

    pop(key: string, defaultValue?: Test_StructureS): Test_StructureS | undefined {
        const value = this.vprValue.pop(key, defaultValue === undefined ? undefined : defaultValue.vprValue);
        return value === undefined ? undefined : Test_StructureS.wrap(value);
    }

    popitem(): [string, Test_StructureS] {
        const entry = this.vprValue.popitem() as unknown as dsviper.OutputValue[];
        return [entry[0] as string, Test_StructureS.wrap(entry[1])];
    }

    setdefault(key: string, defaultValue: Test_StructureS): void {
        this.vprValue.setdefault(key, defaultValue.vprValue);
    }

    update(other: Map_string_to_Test_StructureS): void {
        this.vprValue.update(other.vprValue);
    }

    clear(): void {
        this.vprValue.clear();
    }

    *keys() {
        for (const [k] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield k as string;
        }
    }

    *values() {
        for (const [, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield Test_StructureS.wrap(value);
        }
    }

    *entries(): Generator<[string, Test_StructureS]> {
        for (const [k, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield [k as string, Test_StructureS.wrap(value)];
        }
    }

    items(): [string, Test_StructureS][] {
        return [...this.entries()];
    }

    *[Symbol.iterator]() {
        yield* this.keys();
    }

    copy(): Map_string_to_Test_StructureS {
        return new Map_string_to_Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Map_string_to_Test_StructureS {
        const value = dsviper.ValueMap.cast(dsviper.Value.decode(blob, mt.type_map_string_to_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Map_string_to_Test_StructureS(value);
    }
}

// map<uint8, string>
export class Map_uint8_to_string extends Proxy<dsviper.ValueMap> {

    constructor(value?: dsviper.ValueMap | dsviper.InputValue) {
        if (value instanceof dsviper.ValueMap) {
            if (!value.type().equals(mt.type_map_uint8_to_string())) {
                throw new TypeError("value is not a map<uint8, string>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMap(mt.type_map_uint8_to_string(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Map_uint8_to_string {
        return new Map_uint8_to_string(dsviper.ValueMap.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(key: number): string {
        return this.vprValue.at(key) as string;
    }

    set(key: number, value: string): void {
        this.vprValue.set(key, value);
    }

    has(key: number): boolean {
        return this.vprValue.contains(key);
    }

    get(key: number, defaultValue?: string): string | undefined {
        const value = this.vprValue.get(key, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    remove(key: number): void {
        this.vprValue.remove(key);
    }

    pop(key: number, defaultValue?: string): string | undefined {
        const value = this.vprValue.pop(key, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    popitem(): [number, string] {
        const entry = this.vprValue.popitem() as unknown as dsviper.OutputValue[];
        return [entry[0] as number, entry[1] as string];
    }

    setdefault(key: number, defaultValue: string): void {
        this.vprValue.setdefault(key, defaultValue);
    }

    update(other: Map_uint8_to_string): void {
        this.vprValue.update(other.vprValue);
    }

    clear(): void {
        this.vprValue.clear();
    }

    *keys() {
        for (const [k] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield k as number;
        }
    }

    *values() {
        for (const [, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield value as string;
        }
    }

    *entries(): Generator<[number, string]> {
        for (const [k, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield [k as number, value as string];
        }
    }

    items(): [number, string][] {
        return [...this.entries()];
    }

    *[Symbol.iterator]() {
        yield* this.keys();
    }

    copy(): Map_uint8_to_string {
        return new Map_uint8_to_string(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Map_uint8_to_string {
        const value = dsviper.ValueMap.cast(dsviper.Value.decode(blob, mt.type_map_uint8_to_string(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Map_uint8_to_string(value);
    }
}

// map<vector<Test::StructureS>, string>
export class Map_Vector_Test_StructureS_to_string extends Proxy<dsviper.ValueMap> {

    constructor(value?: dsviper.ValueMap | dsviper.InputValue) {
        if (value instanceof dsviper.ValueMap) {
            if (!value.type().equals(mt.type_map_vector_Test_StructureS_to_string())) {
                throw new TypeError("value is not a map<vector<Test::StructureS>, string>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMap(mt.type_map_vector_Test_StructureS_to_string(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): Map_Vector_Test_StructureS_to_string {
        return new Map_Vector_Test_StructureS_to_string(dsviper.ValueMap.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(key: Vector_Test_StructureS): string {
        return this.vprValue.at(key.vprValue) as string;
    }

    set(key: Vector_Test_StructureS, value: string): void {
        this.vprValue.set(key.vprValue, value);
    }

    has(key: Vector_Test_StructureS): boolean {
        return this.vprValue.contains(key.vprValue);
    }

    get(key: Vector_Test_StructureS, defaultValue?: string): string | undefined {
        const value = this.vprValue.get(key.vprValue, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    remove(key: Vector_Test_StructureS): void {
        this.vprValue.remove(key.vprValue);
    }

    pop(key: Vector_Test_StructureS, defaultValue?: string): string | undefined {
        const value = this.vprValue.pop(key.vprValue, defaultValue === undefined ? undefined : defaultValue);
        return value === undefined ? undefined : value as string;
    }

    popitem(): [Vector_Test_StructureS, string] {
        const entry = this.vprValue.popitem() as unknown as dsviper.OutputValue[];
        return [Vector_Test_StructureS.wrap(entry[0]), entry[1] as string];
    }

    setdefault(key: Vector_Test_StructureS, defaultValue: string): void {
        this.vprValue.setdefault(key.vprValue, defaultValue);
    }

    update(other: Map_Vector_Test_StructureS_to_string): void {
        this.vprValue.update(other.vprValue);
    }

    clear(): void {
        this.vprValue.clear();
    }

    *keys() {
        for (const [k] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield Vector_Test_StructureS.wrap(k);
        }
    }

    *values() {
        for (const [, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield value as string;
        }
    }

    *entries(): Generator<[Vector_Test_StructureS, string]> {
        for (const [k, value] of this.vprValue as unknown as Iterable<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield [Vector_Test_StructureS.wrap(k), value as string];
        }
    }

    items(): [Vector_Test_StructureS, string][] {
        return [...this.entries()];
    }

    *[Symbol.iterator]() {
        yield* this.keys();
    }

    copy(): Map_Vector_Test_StructureS_to_string {
        return new Map_Vector_Test_StructureS_to_string(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Map_Vector_Test_StructureS_to_string {
        const value = dsviper.ValueMap.cast(dsviper.Value.decode(blob, mt.type_map_vector_Test_StructureS_to_string(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Map_Vector_Test_StructureS_to_string(value);
    }
}

// xarray<Test::StructureS>
export class XArray_Test_StructureS extends Proxy<dsviper.ValueXArray> {

    constructor(value?: dsviper.ValueXArray | dsviper.InputValue) {
        if (value instanceof dsviper.ValueXArray) {
            if (!value.type().equals(mt.type_xarray_Test_StructureS())) {
                throw new TypeError("value is not a xarray<Test::StructureS>");
            }
            super(value);
        } else {
            super(new dsviper.ValueXArray(mt.type_xarray_Test_StructureS(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): XArray_Test_StructureS {
        return new XArray_Test_StructureS(dsviper.ValueXArray.cast(value as dsviper.Value));
    }

    static end(): dsviper.ValueUUId {
        return dsviper.ValueXArray.END;
    }

    static createPosition(): dsviper.ValueUUId {
        return dsviper.ValueXArray.createPosition();
    }

    get size(): number {
        return this.vprValue.items().length;
    }

    at(position: dsviper.ValueUUId): Test_StructureS | undefined {
        const v = this.vprValue.at(position);
        return v === undefined ? undefined : Test_StructureS.wrap(v);
    }

    get(key: number | dsviper.ValueUUId): Test_StructureS | undefined {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return undefined; }
        return this.at(position);
    }

    set(key: number | dsviper.ValueUUId, value: Test_StructureS): void {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return; }
        this.vprValue.set(position, value.vprValue);
    }

    contains(value: Test_StructureS): boolean {
        return this.vprValue.positionOf(value.vprValue) !== undefined;
    }

    append(value: Test_StructureS): dsviper.ValueUUId {
        return this.vprValue.append(value.vprValue);
    }

    insert(beforePosition: dsviper.ValueUUId, value: Test_StructureS, newPosition?: dsviper.ValueUUId): dsviper.ValueUUId {
        if (newPosition === undefined) {
            return this.vprValue.insert(beforePosition, value.vprValue);
        }
        return this.vprValue.insert(beforePosition, value.vprValue, newPosition);
    }

    remove(position: dsviper.ValueUUId): void {
        this.vprValue.remove(position);
    }

    positions(): dsviper.ValueUUId[] {
        return this.vprValue.positions();
    }

    index(position: dsviper.ValueUUId): number | undefined {
        return this.vprValue.index(position);
    }

    position(index: number): dsviper.ValueUUId | undefined {
        return this.vprValue.position(index);
    }

    hasPosition(position: dsviper.ValueUUId): boolean {
        return this.vprValue.hasPosition(position);
    }

    insertPosition(beforePosition: dsviper.ValueUUId, newPosition: dsviper.ValueUUId): void {
        this.vprValue.insertPosition(beforePosition, newPosition);
    }

    disablePosition(position: dsviper.ValueUUId): void {
        this.vprValue.disablePosition(position);
    }

    positionOf(value: Test_StructureS): dsviper.ValueUUId | undefined {
        return this.vprValue.positionOf(value.vprValue);
    }

    items(): [dsviper.ValueUUId, Test_StructureS][] {
        return this.vprValue.items().map(([position, value]) => [position, Test_StructureS.wrap(value as dsviper.OutputValue)]);
    }

    toVector(): Vector_Test_StructureS {
        return new Vector_Test_StructureS(this.vprValue.toVector());
    }

    copy(): XArray_Test_StructureS {
        return new XArray_Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): XArray_Test_StructureS {
        const value = dsviper.ValueXArray.cast(dsviper.Value.decode(blob, mt.type_xarray_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new XArray_Test_StructureS(value);
    }
}

// xarray<int8>
export class XArray_int8 extends Proxy<dsviper.ValueXArray> {

    constructor(value?: dsviper.ValueXArray | dsviper.InputValue) {
        if (value instanceof dsviper.ValueXArray) {
            if (!value.type().equals(mt.type_xarray_int8())) {
                throw new TypeError("value is not a xarray<int8>");
            }
            super(value);
        } else {
            super(new dsviper.ValueXArray(mt.type_xarray_int8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): XArray_int8 {
        return new XArray_int8(dsviper.ValueXArray.cast(value as dsviper.Value));
    }

    static end(): dsviper.ValueUUId {
        return dsviper.ValueXArray.END;
    }

    static createPosition(): dsviper.ValueUUId {
        return dsviper.ValueXArray.createPosition();
    }

    get size(): number {
        return this.vprValue.items().length;
    }

    at(position: dsviper.ValueUUId): number | undefined {
        const v = this.vprValue.at(position);
        return v === undefined ? undefined : v as number;
    }

    get(key: number | dsviper.ValueUUId): number | undefined {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return undefined; }
        return this.at(position);
    }

    set(key: number | dsviper.ValueUUId, value: number): void {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return; }
        this.vprValue.set(position, value);
    }

    contains(value: number): boolean {
        return this.vprValue.positionOf(value) !== undefined;
    }

    append(value: number): dsviper.ValueUUId {
        return this.vprValue.append(value);
    }

    insert(beforePosition: dsviper.ValueUUId, value: number, newPosition?: dsviper.ValueUUId): dsviper.ValueUUId {
        if (newPosition === undefined) {
            return this.vprValue.insert(beforePosition, value);
        }
        return this.vprValue.insert(beforePosition, value, newPosition);
    }

    remove(position: dsviper.ValueUUId): void {
        this.vprValue.remove(position);
    }

    positions(): dsviper.ValueUUId[] {
        return this.vprValue.positions();
    }

    index(position: dsviper.ValueUUId): number | undefined {
        return this.vprValue.index(position);
    }

    position(index: number): dsviper.ValueUUId | undefined {
        return this.vprValue.position(index);
    }

    hasPosition(position: dsviper.ValueUUId): boolean {
        return this.vprValue.hasPosition(position);
    }

    insertPosition(beforePosition: dsviper.ValueUUId, newPosition: dsviper.ValueUUId): void {
        this.vprValue.insertPosition(beforePosition, newPosition);
    }

    disablePosition(position: dsviper.ValueUUId): void {
        this.vprValue.disablePosition(position);
    }

    positionOf(value: number): dsviper.ValueUUId | undefined {
        return this.vprValue.positionOf(value);
    }

    items(): [dsviper.ValueUUId, number][] {
        return this.vprValue.items().map(([position, value]) => [position, value as dsviper.OutputValue as number]);
    }

    toVector(): Vector_int8 {
        return new Vector_int8(this.vprValue.toVector());
    }

    copy(): XArray_int8 {
        return new XArray_int8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): XArray_int8 {
        const value = dsviper.ValueXArray.cast(dsviper.Value.decode(blob, mt.type_xarray_int8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new XArray_int8(value);
    }
}

// xarray<uint8>
export class XArray_uint8 extends Proxy<dsviper.ValueXArray> {

    constructor(value?: dsviper.ValueXArray | dsviper.InputValue) {
        if (value instanceof dsviper.ValueXArray) {
            if (!value.type().equals(mt.type_xarray_uint8())) {
                throw new TypeError("value is not a xarray<uint8>");
            }
            super(value);
        } else {
            super(new dsviper.ValueXArray(mt.type_xarray_uint8(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): XArray_uint8 {
        return new XArray_uint8(dsviper.ValueXArray.cast(value as dsviper.Value));
    }

    static end(): dsviper.ValueUUId {
        return dsviper.ValueXArray.END;
    }

    static createPosition(): dsviper.ValueUUId {
        return dsviper.ValueXArray.createPosition();
    }

    get size(): number {
        return this.vprValue.items().length;
    }

    at(position: dsviper.ValueUUId): number | undefined {
        const v = this.vprValue.at(position);
        return v === undefined ? undefined : v as number;
    }

    get(key: number | dsviper.ValueUUId): number | undefined {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return undefined; }
        return this.at(position);
    }

    set(key: number | dsviper.ValueUUId, value: number): void {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return; }
        this.vprValue.set(position, value);
    }

    contains(value: number): boolean {
        return this.vprValue.positionOf(value) !== undefined;
    }

    append(value: number): dsviper.ValueUUId {
        return this.vprValue.append(value);
    }

    insert(beforePosition: dsviper.ValueUUId, value: number, newPosition?: dsviper.ValueUUId): dsviper.ValueUUId {
        if (newPosition === undefined) {
            return this.vprValue.insert(beforePosition, value);
        }
        return this.vprValue.insert(beforePosition, value, newPosition);
    }

    remove(position: dsviper.ValueUUId): void {
        this.vprValue.remove(position);
    }

    positions(): dsviper.ValueUUId[] {
        return this.vprValue.positions();
    }

    index(position: dsviper.ValueUUId): number | undefined {
        return this.vprValue.index(position);
    }

    position(index: number): dsviper.ValueUUId | undefined {
        return this.vprValue.position(index);
    }

    hasPosition(position: dsviper.ValueUUId): boolean {
        return this.vprValue.hasPosition(position);
    }

    insertPosition(beforePosition: dsviper.ValueUUId, newPosition: dsviper.ValueUUId): void {
        this.vprValue.insertPosition(beforePosition, newPosition);
    }

    disablePosition(position: dsviper.ValueUUId): void {
        this.vprValue.disablePosition(position);
    }

    positionOf(value: number): dsviper.ValueUUId | undefined {
        return this.vprValue.positionOf(value);
    }

    items(): [dsviper.ValueUUId, number][] {
        return this.vprValue.items().map(([position, value]) => [position, value as dsviper.OutputValue as number]);
    }

    toVector(): Vector_uint8 {
        return new Vector_uint8(this.vprValue.toVector());
    }

    copy(): XArray_uint8 {
        return new XArray_uint8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): XArray_uint8 {
        const value = dsviper.ValueXArray.cast(dsviper.Value.decode(blob, mt.type_xarray_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new XArray_uint8(value);
    }
}

// variant<string, uint8, Test::StructureS>
export class Variant_string_uint8_Test_StructureS extends Proxy<dsviper.ValueVariant> {

    constructor(value?: dsviper.ValueVariant | Proxy | dsviper.InputValue) {
        if (value === undefined || value === null) {
            super(new dsviper.ValueVariant(mt.type_variant_string_uint8_Test_StructureS()));
        } else if (value instanceof dsviper.ValueVariant) {
            if (!value.type().equals(mt.type_variant_string_uint8_Test_StructureS())) {
                throw new TypeError("value is not a variant<string, uint8, Test::StructureS>");
            }
            super(value);
        } else if (value instanceof Proxy) {
            super(new dsviper.ValueVariant(mt.type_variant_string_uint8_Test_StructureS(), value.vprValue));
        } else {
            super(new dsviper.ValueVariant(mt.type_variant_string_uint8_Test_StructureS(), value as dsviper.InputValue));
        }
    }

    static wrap(value: dsviper.OutputValue): Variant_string_uint8_Test_StructureS {
        return new Variant_string_uint8_Test_StructureS(dsviper.ValueVariant.cast(value as dsviper.Value));
    }

    isString(): boolean {
        return (this.vprValue.unwrap(false) as dsviper.Value).type().equals(mt.type_string());
    }

    getString(): string {
        if (!this.isString()) {
            throw new Error("variant does not hold a string");
        }
        return this.vprValue.unwrap() as string;
    }

    setString(value: string): void {
        this.vprValue.wrap(value, mt.type_string());
    }
    isUint8(): boolean {
        return (this.vprValue.unwrap(false) as dsviper.Value).type().equals(mt.type_uint8());
    }

    getUint8(): number {
        if (!this.isUint8()) {
            throw new Error("variant does not hold a number");
        }
        return this.vprValue.unwrap() as number;
    }

    setUint8(value: number): void {
        this.vprValue.wrap(value, mt.type_uint8());
    }
    isTest_StructureS(): boolean {
        return (this.vprValue.unwrap(false) as dsviper.Value).type().equals(mt.type_Test_StructureS());
    }

    getTest_StructureS(): Test_StructureS {
        if (!this.isTest_StructureS()) {
            throw new Error("variant does not hold a Test_StructureS");
        }
        return Test_StructureS.wrap(this.vprValue.unwrap());
    }

    setTest_StructureS(value: Test_StructureS): void {
        this.vprValue.wrap(value.vprValue, mt.type_Test_StructureS());
    }

    copy(): Variant_string_uint8_Test_StructureS {
        return new Variant_string_uint8_Test_StructureS(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Variant_string_uint8_Test_StructureS {
        const value = dsviper.ValueVariant.cast(dsviper.Value.decode(blob, mt.type_variant_string_uint8_Test_StructureS(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Variant_string_uint8_Test_StructureS(value);
    }
}

// variant<string, uint8>
export class Variant_string_uint8 extends Proxy<dsviper.ValueVariant> {

    constructor(value?: dsviper.ValueVariant | Proxy | dsviper.InputValue) {
        if (value === undefined || value === null) {
            super(new dsviper.ValueVariant(mt.type_variant_string_uint8()));
        } else if (value instanceof dsviper.ValueVariant) {
            if (!value.type().equals(mt.type_variant_string_uint8())) {
                throw new TypeError("value is not a variant<string, uint8>");
            }
            super(value);
        } else if (value instanceof Proxy) {
            super(new dsviper.ValueVariant(mt.type_variant_string_uint8(), value.vprValue));
        } else {
            super(new dsviper.ValueVariant(mt.type_variant_string_uint8(), value as dsviper.InputValue));
        }
    }

    static wrap(value: dsviper.OutputValue): Variant_string_uint8 {
        return new Variant_string_uint8(dsviper.ValueVariant.cast(value as dsviper.Value));
    }

    isString(): boolean {
        return (this.vprValue.unwrap(false) as dsviper.Value).type().equals(mt.type_string());
    }

    getString(): string {
        if (!this.isString()) {
            throw new Error("variant does not hold a string");
        }
        return this.vprValue.unwrap() as string;
    }

    setString(value: string): void {
        this.vprValue.wrap(value, mt.type_string());
    }
    isUint8(): boolean {
        return (this.vprValue.unwrap(false) as dsviper.Value).type().equals(mt.type_uint8());
    }

    getUint8(): number {
        if (!this.isUint8()) {
            throw new Error("variant does not hold a number");
        }
        return this.vprValue.unwrap() as number;
    }

    setUint8(value: number): void {
        this.vprValue.wrap(value, mt.type_uint8());
    }

    copy(): Variant_string_uint8 {
        return new Variant_string_uint8(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): Variant_string_uint8 {
        const value = dsviper.ValueVariant.cast(dsviper.Value.decode(blob, mt.type_variant_string_uint8(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new Variant_string_uint8(value);
    }
}
