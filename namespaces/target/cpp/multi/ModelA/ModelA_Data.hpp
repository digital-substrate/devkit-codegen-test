// Generated by kibo from Topology. Do not edit by hand.
// ... same banner ...

// A DRIVER UNIT. Everything inside is unqualified: Material, not ModelA_Material.
// One include, path-less, resolved by one -I per unit -- the convention viper,
// ge and red already follow for generated and hand-written code alike.

#ifndef ModelA_Data_hpp
#define ModelA_Data_hpp

#include "Topology_Data.hpp"        // the base unit: AnyConceptKey
#include "Viper_UUId.hpp"
#include <cstdint>

namespace ModelA {

class MaterialKey;

class MaterialKey final {
    // ... unchanged; toAnyConceptKey() returns Topology::AnyConceptKey ...
};

class Colour final {
    // ... unchanged: uint8 r, g, b ...
};

} // ns ModelA

// Only this unit's own key types. ModelB's are specialised in ModelB_Data.hpp.
template<> struct std::hash<ModelA::MaterialKey> {
    std::size_t operator()(ModelA::MaterialKey const & v) const noexcept { return v.hash(); }
};

#endif
