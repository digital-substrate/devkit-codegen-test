// Generated by kibo from Topology. Do not edit by hand.
// Templates: kibo-template-viper 2.0.0 (MIT), Template Model 2.
// Runtime: this file links against the `viper` C++ runtime 1.2.x, ...

// THE BASE UNIT. -n names this unit; it is NOT an enclosing scope for the others.
// AnyConceptKey carries no model dependency at all -- it is (instanceId, runtimeId)
// and belongs in the runtime. It is emitted here only because viper 1.2's API is
// closed. Every other unit includes this one; it includes none of them.

#ifndef Topology_Data_hpp
#define Topology_Data_hpp

#include "Viper_UUId.hpp"

namespace Topology {

// MARK: - any_concept
class AnyConceptKey final {
    // ... unchanged ...
};

} // ns Topology

template<> struct std::hash<Topology::AnyConceptKey> {
    std::size_t operator()(Topology::AnyConceptKey const & v) const noexcept { return v.hash(); }
};

#endif
